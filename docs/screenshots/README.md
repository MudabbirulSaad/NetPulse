# Public Screenshots

`dashboard.png` is a real application capture using only the three public first-run demonstration targets. It contains no student details, credentials, private endpoints, or local file paths.

Assessment-only captures and images containing identifying information belong in `docs/screenshots/private/`, which is ignored by Git, or outside the repository.
